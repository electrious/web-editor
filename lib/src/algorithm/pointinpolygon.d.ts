import { Vector2 } from 'three';
/**
 * test if a 2d point is inside a polygon
 * @param polygon
 * @param point
 */
export declare function pointInPolygon(polygon: Vector2[], point: Vector2): boolean;
